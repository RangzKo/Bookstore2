<h2 class="page-header">Recently Added</h2>
<div>
  <% books.forEach(book => { %>
    <a href="/books/<%= book.id %>">
      <img height="140" width="90" src="<%= book.coverImagePath %>">
    </a>
  <% }) %>
</div>


